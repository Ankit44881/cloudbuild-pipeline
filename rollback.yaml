steps:
  - name: 'gcr.io/cloud-builders/gke-deploy'
    args:
      - run
      - --filename=kube.yaml
      - --image=us-east1-docker.pkg.dev/$PROJECT_ID/my-repo/my-app:${_IMAGE_TAG}
      - --location=us-east1-b
      - --cluster=devops-cluster

options:
  logging: CLOUD_LOGGING_ONLY